pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "C-c-to-app"
include ':app'
include ':native-lib'

// This explicitly points Gradle to your exact folder name
project(':native-lib').projectDir = new File(settingsDir, 'native-lib')
